# Source Heavy Fixture

This fixture represents a repository where TypeScript source and tests dominate
the context window. It exists to exercise source category totals separately from
documentation-heavy scans.
